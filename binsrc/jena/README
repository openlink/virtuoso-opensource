How to build Virtuoso Jena Provider
===================================

* Create a lib directory and put the following .jar file from the openrdf 
  project in here:

	arq.jar
	axis.jar
	iri.jar
	jena.jar

* The following .jar files are needed for running the samples:

	commons-logging-1.1.jar
	icu4j_3_4.jar
	xercesImpl.jar


* Run the make command
	
	make virt_jena.jar
