The files in this directory are documentation files for WebCalendar developers.

If this directory is empty, it can be regenerated by simply typing "make" on
the command line (assuming you have perl installed).

database.html
  Describes the database tables in WebCalendar.

